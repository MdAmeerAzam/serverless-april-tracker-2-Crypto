name: DISABLED - Audit Sheets (was purge_db - DROP TABLE risk)
# DISABLED: This workflow ran purge_db.js which drops database tables.
# It has been disabled permanently. Do not re-enable without full review.
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. It previously ran purge_db.js (DROP TABLE). Contact admin before re-enabling."