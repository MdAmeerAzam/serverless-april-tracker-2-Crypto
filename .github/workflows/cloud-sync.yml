name: Cloud Sync and Backup (RETIRED - use sync.yml backup.yml watchdog.yml)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. Use sync.yml, backup.yml, watchdog.yml instead."
